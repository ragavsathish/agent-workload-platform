# Adapters

Concrete implementations at external seams live here: browsers, storage,
model providers, command runners, and other environment-dependent capabilities.

`mermaid-to-excalidraw` is an untouched upstream submodule. `c4-gondolin` is
the optional browser compatibility adapter; normal C4 layout runs in Wasm.
