# Components

Reusable WIT/WebAssembly modules live here after at least two workloads depend
on the same interface and implementation. Workload-specific modules remain
inside their workload directory.
